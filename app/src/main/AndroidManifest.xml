<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mary">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.EditActivity"></activity>
        <activity android:name=".ui.activity.Student3Activity" />
        <activity android:name=".ui.activity.Teacher2Activity" />
        <activity android:name=".ui.activity.Student2Activity" />
        <activity android:name=".ui.activity.PeopleActivity" />
        <activity android:name=".ui.activity.TeacherActivity" />
        <activity android:name=".ui.activity.AdminActivity" />
        <activity
            android:name=".ui.activity.MessageActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".ui.activity.InfoManActivity" />
        <activity android:name=".ui.activity.AdaptationActivity" />
        <activity android:name=".ui.activity.EventActivity" />
        <activity android:name=".ui.activity.StudentActivity" />
        <activity android:name=".ui.activity.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>